# falamus prompt fragments (cloud set) — working rules kept minimal for a capable model

## common
<!-- scope: rules every agent sees (cloud: blank — a capable model doesn't need the coaching) -->

## delegate
<!-- scope: delegation rules (cloud: blank — the persona already covers it) -->

## path
<!-- scope: the one falamus-specific gotcha worth keeping -->
- To read EXISTING project files (outside the shared workspace), use their ABSOLUTE path.

## memo
<!-- scope: memo protocol (cloud: blank — the memo tool's own description suffices) -->
